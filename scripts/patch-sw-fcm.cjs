/**
 * DEPRECATED
 * O importScripts do FCM agora é responsabilidade do next-pwa via next.config.js (pwa.importScripts).
 * Este script foi mantido apenas para compatibilidade, mas não faz mais nada.
 */
console.log("ℹ️ scripts/patch-sw-fcm.cjs (deprecated): FCM importScripts é gerenciado pelo next-pwa/next.config.js. Nada a fazer.");
process.exit(0);
