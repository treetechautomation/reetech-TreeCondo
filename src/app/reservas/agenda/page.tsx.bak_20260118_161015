import AppLayout from "@/components/layout/AppLayout";

export default function ReservasAgendaPage() {
  return (
    <AppLayout pageTitle="Agenda de Reservas">
      <div className="rounded-2xl border border-black/10 bg-white p-6">
        <div className="text-lg font-semibold text-slate-900">Agenda de Reservas</div>
        <p className="mt-2 text-slate-600">
          Em breve: visão de calendário (read-only) + botão de registrar entrada (check-in) para o porteiro.
        </p>
      </div>
    </AppLayout>
  );
}
