import type { Viewport } from "next";

export const viewport: Viewport = {
  themeColor: "#00BEEA",
  colorScheme: "dark",
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  viewportFit: "cover",
};
