"use client";

import AppLayout from "@/components/layout/AppLayout";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function AdministradorGlobalPage() {
  return (
    <AppLayout pageTitle="Administrador Global">
      <Card>
        <CardHeader>
          <CardTitle>Administrador Global</CardTitle>
          <CardDescription>
            Controle global do sistema (menus, permissões, configurações do condomínio).
          </CardDescription>
        </CardHeader>
        <CardContent>
          <div className="text-sm text-slate-600">
            (Página em construção. Aqui entra a tela de permissões de menu por perfil/condomínio.)
          </div>
        </CardContent>
      </Card>
    </AppLayout>
  );
}
